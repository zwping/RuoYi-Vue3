name: Sync Fork

on:
  schedule:
    - cron: '* 22 * * *' # every day
  workflow_dispatch: # on button click

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v1.6.3
        with:
          owner: yangzongzhuan
          base: master
          head: master
